<%# The site's formatter derives the preview component from the doc filename, so
    a <!-- preview:marks --> tag in docs/theming.md resolves to
    previews/theming/marks. The specimen page itself lives in previews/specimen —
    this is the same page under the path the docs contract requires. %>
<%= render template: "previews/specimen/marks" %>
